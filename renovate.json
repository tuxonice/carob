{
    "extends": [
        "config:base"
    ],
    "rangeStrategy": "pin",
    "baseBranches": ["dependency-updates"],
    "packageRules": [
        {
            "updateTypes": ["patch", "pin", "digest"],
            "automerge": true
        }
    ]
}
